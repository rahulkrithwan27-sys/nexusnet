# Workspace benchmarks

Cross-crate and end-to-end Criterion benchmarks live here in later phases.

Per-crate benchmarks live in each crate's own `benches/` directory, e.g.
[`crates/core/benches/`](../crates/core/benches/). Run them with:

```bash
cargo bench -p nexusnet-core
```
